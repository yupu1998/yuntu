

/**
 * 基本地形模型
 * HIGH_BASE
 * BASE
 * LOWER_BASE
 * NO_BASE
 */
export enum baseGroudModel{
    HIGH_BASE,
    BASE,
    LOWER_BASE,
    NO_BASE
}

/**
 * 地面装饰花纹
 */
export enum decorativePattern{
    BASE_GROUND,
}

/**
 * buff装饰花纹
 */
export enum BuffPattern{
    NP_BUFF,
    SNIPE
}

/**
 * 平台建筑
 */
export enum BuildModel{
   NO_BUILD
}